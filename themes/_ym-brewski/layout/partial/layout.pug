doctype
html(lang=config.language)
    head
        include head.pug
    body
        .wrap#barba-wrapper
            header
                h1.branding= config.title
                include nav.pug
            .barba-container
                main.container
                    block container
                footer
                    block pagination
                    block copyright
        include scripts.pug
